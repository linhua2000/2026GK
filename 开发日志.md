# 开发日志

## 2026-09-16
- CubeMX 配好外设骨架并首次提交代码：GPIO / USART1.2.3.6 / UART4.5 / CAN1.2 / TIM1-4.6.8-12 / SPI2 / I2C3 / ADC1.2
- 封装 LED 模块（`Middle_Drivers/LED`）：LED1=PB2、LED2=PC0、LED3=PE1、LED4=PD6
- 裁剪中断：`stm32f4xx_it.c` 只留 USART1 和 TIM6

## 2026-09-17
- 封装 uart1 蓝牙串口（`Middle_Drivers/uart1`）：USART1 PA9/PA10，115200，阻塞发送

## 2026-09-18
- `app/control.c`：TIM6 中断回调，5ms 跑一次
- 封装四路编码器（`Middle_Drivers/encoder`）：TIM1/TIM2/TIM3/TIM4，读增量换算成脉冲/秒
- 新建 TB6612 电机驱动（`Middle_Drivers/motor`）：四路 PWM + 八根方向脚，`Motor_Load()` 一次写四路
- 调引脚：I2C1 去掉只留 I2C3；PD7 从 EXTI 改成普通输出（给电机当方向脚）

## 2026-09-19
- `tim.c`：三路电机 PWM 频率统一到 20kHz（TIM10 PSC=7/ARR=1049、TIM11 PSC=7、TIM12 PSC=3 —— APB1 上的定时器时钟只有 84MHz）
- `main.c`：蓝牙每 100ms 上报四轮速度 `E1:.. E2:.. E3:.. E4:..`
- 修正「轮号 → 编码器定时器」映射：轮1=TIM1、轮2=TIM3、轮3=TIM2、轮4=TIM4（`分配表.md` 里的编号是理论分配，和实车接线不一致）

## 2026-09-20
- 新建 `Middle_Drivers/kinematics`（麦轮逆运动学）：`Set_Vel(vx, vy, ω)` 只存指令，`Exp_Speed_Cal()` 解算成四轮 RPM；超限时四路按同一比例缩放，不逐路钳位（保住轮间比例，旋转分量不会被削掉）
- 新建 `Middle_Drivers/mailuncontrol`（四路速度环 PI）：误差低通 a=0.8、积分限幅 ±10000；增益标定为 Kp=2.6 / Ki=0.08
- `app/control.c`：蓝牙指令改成手写解析 `#N v` / `#a v1 v2 v3 v4`（不用 sscanf —— 解析跑在 USART1 中断里，不能和主循环的 sprintf 抢 stdio）

## 2026-09-21
- `kinematics.c`：补 `Kinematics_Pulse_To_RPM` / `Kinematics_RPM_To_Pulse` 双向换算（基准 `ENCODER_PPR = 60000`，公式里单列该宏，将来实测出真值只改宏）
- `app/control.c`：TIM6 的 5ms 中断里串通 编码器 → `Exp_Speed_Cal()` → 速度环 → `Motor_Load`
- `main.c`：补 `Kinematics_Init()` —— 漏调用会让 `wheel_circumference_` 保持 0，解算出 NaN，四轮一动不动
- 排查「前进时前左轮不转、后退时后左轮不转」：**定位到板子上 PD11 和 PD15 短接**。H1 两个通道的 IN2 被并成一根线，两脚推挽对顶后电平失控；左侧两路的 IN2 实际恒低，命令为正时 IN1=IN2 → 短路刹车，所以「画 Y 时左双轮只有一个能转」
